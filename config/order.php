<?php

/*
|--------------------------------------------------------------------------
| ค่าตั้งของเมนู O-Order (ใบสั่งซื้อ)
|--------------------------------------------------------------------------
|
| ⚠ md_password — รหัสผ่านสำหรับกดอนุมัติในฟอร์ม "MK ขออนุมัติราคาพิเศษ"
|   ฟอร์ม Access เดิมมีช่อง "รหัสผ่าน MD (เพื่ออนุมัติ)" แต่ยังหาไม่เจอว่าเก็บไว้ที่ไหนในฐานข้อมูล
|   จึงเดาว่าเป็นรหัสเดียวใช้ร่วมกัน แล้วเก็บไว้ที่นี่ก่อน (ตั้งทับได้ด้วย ORDER_MD_PASSWORD ใน .env)
|
|   ได้ข้อมูลจริงเมื่อไหร่ ให้ย้ายไปอ่านจากที่เก็บจริง แล้วแก้ที่ PriceApprovalController::checkMdPassword()
|   ที่เดียว — จุดอื่นไม่ต้องแก้
|
*/

return [

    'md_password' => env('ORDER_MD_PASSWORD', 'md1234'),

];
