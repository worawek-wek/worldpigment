<?php

return [

    /*
    |--------------------------------------------------------------------------
    | วันหยุดประจำสัปดาห์
    |--------------------------------------------------------------------------
    |
    | เลขวันตาม PHP date('w') — 0 = อาทิตย์, 1 = จันทร์, ... 6 = เสาร์
    |
    | ค่าปัจจุบัน [0] = หยุดเฉพาะวันอาทิตย์ (โรงงานทำงานวันเสาร์) — ผู้ใช้ยืนยัน 01/09/2569
    | ถ้าภายหลังหยุดเสาร์ด้วยให้ใส่เป็น [0, 6] · ไม่หยุดประจำสัปดาห์เลยใส่ []
    |
    | ใช้ที่ App\Services\HolidayService (นับวันทำการ / ข้ามวันหยุด)
    |
    */
    'weekly_off' => [0],

];
